<!DOCTYPE html>
<html>
  <head>
    <%= render "partials/head" %>
  </head>
  <body class="cms-index-index front <%= current_page?(root_url) ? 'cms-home' : '' %>">
    <div class="wrapper">
      <div class="page">
        <%= yield %>
        <%= render "partials/footer" %>
      </div>
    </div>
  </body>
</html>
